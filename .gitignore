# Python
.venv/
__pycache__/
*.pyc

# Secrets — never commit these
.env
.env.local
# Streamlit Cloud reads secrets from its dashboard; this is the local equivalent
# and must never be committed.
.streamlit/secrets.toml

# Editors / OS
.idea/
.DS_Store
